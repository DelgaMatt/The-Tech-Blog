<section class="card">
    <div class="post-title">
        <div>{{title}}</div>
        <div class="post-tag">created by {{user.username}}</div>
    </div>
    <div class="post-text">
        {{post_text}}
    </div>
</section>